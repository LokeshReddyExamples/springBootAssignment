spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: 'jdbc:mysql://localhost:3306/spring_boot'
    username: root
    password: Emids123
'#spring':
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: 'true'
